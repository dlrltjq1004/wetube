extends layouts/main

block content
    .form-container#jsRecordContainer
        .record-container
            video#jsVideoPreview
            button#jsRecordBtn 비디오 녹화
        form(action=`/videos${routes.upload}`, method="post", enctype="multipart/form-data")
            div.fileUpload
                label(for="file") Video File
                input(type="file", id="file", name="videoFile", required=true, accept="video/*")
            input(type="text", placeholder="제목", name="title", required=true)
            textarea(name="description", placeholder="비디오 소개")
            input(type="submit", value="업로드", required=true)